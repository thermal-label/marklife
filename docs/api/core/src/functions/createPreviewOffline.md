[**marklife**](../../../README.md)

***

[marklife](../../../README.md) / [core/src](../README.md) / createPreviewOffline

# Function: createPreviewOffline()

> **createPreviewOffline**(`image`, `options?`): [`PreviewResult`](/contracts/api/interfaces/PreviewResult)

Offline preview — render the user image with the same dither the
print path uses, into a single black-on-white plane.

## Parameters

### image

[`RawImageData`](/contracts/api/interfaces/RawImageData)

### options?

[`PreviewOptions`](/contracts/api/interfaces/PreviewOptions) = `{}`

## Returns

[`PreviewResult`](/contracts/api/interfaces/PreviewResult)
