[**marklife**](../../../README.md)

***

[marklife](../../../README.md) / [core/src](../README.md) / buildSetGapDistances

# Function: buildSetGapDistances()

> **buildSetGapDistances**(`gap`, `offset`): `Uint8Array`

`SET GAP <gap>,<offset>\r\n` — TSC TSPL II spec extended form.

`gap` and `offset` are in mm.

## Parameters

### gap

`number`

### offset

`number`

## Returns

`Uint8Array`
