[**marklife**](../../../README.md)

***

[marklife](../../../README.md) / [core/src](../README.md) / buildEscJustify

# Function: buildEscJustify()

> **buildEscJustify**(`mode`): `Uint8Array`

Justify — `ESC a n` (0 = left, 1 = center, 2 = right).

Real Epson spec but OOS in `escpos-core@0.2.1` (vendor extension
of bitmap-only scope). Flagged upstream to escpos-core@0.3.0.

## Parameters

### mode

`0` \| `1` \| `2`

## Returns

`Uint8Array`
